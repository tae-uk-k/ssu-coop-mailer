# 메일 자동화 사용설명서

기업 명단을 넣으면 **기업마다 다른 제안서를 만들어 PDF로 붙여 보내는** 프로그램입니다.
숭실대학교 총학생회 대외협력국에서 씁니다.

---

## 처음 한 번만 하면 되는 것

### 1. 프로그램 켜기

폴더에 있는 **`실행하기.bat`** 을 더블클릭하세요. 그게 전부입니다.

터미널을 쓰신다면 이렇게 해도 됩니다.

```text
python run.py
```

exe로 만들어 두었다면 `메일자동화.exe`를 더블클릭하면 됩니다.

빠진 프로그램이 있으면 **무엇이 없는지와 설치 명령을 화면에 알려주고** 창이 닫히지 않습니다.
거기 적힌 `pip install ...` 한 줄을 복사해 실행하면 됩니다.

> **PowerPoint가 깔려 있어야 합니다.** PDF로 바꿀 때 PowerPoint를 씁니다.
> 없으면 아래쪽 상태줄에 빨간 글씨로 알려줍니다.

### 2. Gmail 연결

오른쪽 위 **`○ Gmail 연결하기`** 를 누르면 브라우저가 열립니다. 학생회 계정으로 로그인하고 권한을 허용하세요.

허용하는 권한은 두 가지입니다.

| 권한 | 쓰는 곳 |
| --- | --- |
| 메일 보내기 | 제안서 PDF를 첨부해 발송 |
| 받은 메일 읽기 | 되돌아온 반송 메일을 찾을 때만 |

한 번 연결하면 계속 유지됩니다. 행사가 바뀌어도 다시 안 해도 됩니다.

> 연결이 안 되고 *credentials.json이 없어요* 라고 나오면, 구글에서 받은 `credentials.json`을
> **설정** 폴더에 넣어 주세요.

---

## 행사 하나를 보내는 흐름

왼쪽에 **5단계**가 있습니다. 위에서부터 채우면 됩니다.
끝난 단계는 파란 체크(✓)로 바뀌고, 아래에 `3 / 5 단계`처럼 얼마나 왔는지 나옵니다.

두 번째부터는 아무 단계나 눌러 바로 갈 수 있습니다.

### 행사 만들기

맨 위 행사 칸을 눌러 **`새 행사 만들기`**. 행사 이름을 적습니다. (예: `키움 히어로즈 DAY`)

행사마다 제안서·명단·메일 문구가 따로 저장돼서 섞이지 않습니다.

---

### 1단계 · 파일

제안서 PPT와 기업 명단 엑셀을 올립니다.

- 카드 위로 **파일을 끌어다 놓거나**, `파일 고르기` 버튼을 누릅니다
- 올린 파일은 **앱이 복사해서 보관**합니다. 원본을 옮기거나 이름을 바꿔도 계속 쓸 수 있습니다
- 같은 파일을 다시 올리면 버전이 올라가고 예전 것도 남습니다 (`이전 버전` 버튼)

PPT를 올리면 바로 훑어서 **바꿀 수 있는 자리를 몇 개 찾았는지** 알려줍니다.

---

### 2단계 · 명단

**이메일이 어느 열인지**만 골라 주면 됩니다. 이름이 비슷하면 알아서 골라 둡니다.

- 표에서 **이메일 칸을 눌러 바로 고칠 수 있습니다.** 엑셀을 따로 열 필요가 없습니다
- `행 추가`로 기업을 더 넣을 수 있습니다
- 위쪽에 **보낼 곳 / 빠지는 곳 / 전체**가 숫자로 나옵니다

빠지는 곳은 이유가 함께 표시됩니다.

| 표시 | 뜻 |
| --- | --- |
| 이메일 없음 | 비어 있거나 `미확인` |
| 이메일 형식 오류 | `@`가 없는 등 |
| 이미 보냄 | 지난번에 발송 완료 |
| 반송된 주소 | 되돌아온 적 있음 |
| 제외 품목 | 아래 *제외 열* 설정에 걸림 |

**제외 열**은 특정 분류를 통째로 건너뛸 때 씁니다.
예를 들어 제외 열을 `작은 유형`으로 두고 옆 칸에 `기타`를 적으면, 품목이 '기타'인 기업은 안 보냅니다.

---

### 3단계 · 제안서 ← 이 프로그램의 핵심

PPT에서 **괄호로 묶인 자리**를 찾아 보여줍니다. 각 자리에 명단의 어느 열을 넣을지 고릅니다.

```text
(기업명)          3곳 · 1장, 6장, 8장
   여기에  [ 업체명 ▾ ]  를 넣어요

   … (기업명)의 (제품명)을 집중적으로 노출시킬 수 있어 …      ← 원래 문장
   … 농심의 라면·면류를 집중적으로 노출시킬 수 있어 …          ← 이렇게 바뀝니다
```

- 자리 이름은 **무엇이든 됩니다.** `(기업명)`이든 `(회사명)`이든 `(협찬사)`든 상관없습니다
- 이름이 명단의 열과 같으면 *자동으로 짝지었어요* 라고 미리 골라 둡니다
- 짝짓지 않은 자리는 **노란 테두리**로 표시되고, 그대로 두면 글자가 제안서에 남습니다
  → 본문의 일반 괄호(`(스태프)`, `(강원도 원주)` 등)는 그냥 두면 됩니다

**조사 자동 맞추기**를 켜 두면 받침에 따라 알아서 골라 씁니다.

```text
농심을 / 오뚜기를      농심은 / 오뚜기는
농심이 / 오뚜기가      농심으로 / 오뚜기로 / 팔도로
```

**만들어질 파일 이름**도 이 화면에서 정합니다. 아래 파란 칸을 눌러 넣으면
바로 밑에 실제 이름이 보입니다.

```text
제안서 {{업체명}}
   → 제안서 농심.pdf   ·  45곳 모두 다른 이름이에요.
```

이름에 기업마다 달라지는 값이 없으면 **파일이 서로 덮어써지므로** 노란색으로 알려줍니다.

보내기 전에 **`미리보기 1장 만들기`**로 첫 기업 몫을 실제로 만들어 열어 보세요.
PDF 변환이 잘 되는지도 여기서 미리 확인됩니다.

> PPT의 **슬라이드 노트**에 있는 괄호는 바뀌지 않습니다. 노트는 PDF에 안 나오기 때문입니다.

---

### 4단계 · 메일

왼쪽에 쓰면 **오른쪽에 실제 나갈 모습이 바로 보입니다.** 위쪽 드롭다운으로 기업을 바꿔 가며 확인하세요.

파란 칩을 누르면 커서 자리에 들어갑니다.

| 쓰는 법 | 결과 |
| --- | --- |
| `{{업체명}}` | 농심 |
| `{{작은 유형}}` | 라면·면류 |
| `{{업체명은}}` | 농심**은** / 오뚜기**는** |
| `{{업체명을}}` | 농심**을** / 오뚜기**를** |

조사는 `은/는`, `이/가`, `을/를`, `과/와`, `으로/로`, `아/야` 를 지원합니다.

첨부될 PDF 이름도 미리보기 아래에 나옵니다.

---

### 5단계 · 보내기

확인할 것을 전부 통과하면 **`○○곳에 보내기`** 버튼이 켜집니다.
빠진 게 있으면 무엇을 채워야 하는지 노란 칸에 나옵니다.

**보내는 간격**은 기본 5초입니다. 한꺼번에 몰아 보내면 Gmail이 스팸으로 분류할 수 있어 띄웁니다.

보내는 동안:

- 진행바와 `12 / 45` 로 어디까지 왔는지 보입니다
- 기업마다 **네 칸짜리 막대**로 단계가 보입니다 — 제안서 → PDF → 명단 기록 → 메일
  - 초록 = 됨, 노랑 = 하는 중, 빨강 = 실패
- **`중지`**를 누르면 멈춥니다. 다시 시작하면 **이미 보낸 곳은 건너뜁니다**

끝나면 보냄/실패 숫자와 함께, 실패한 곳은 **왜 실패했고 어떻게 하면 되는지** 나옵니다.

---

### 보낸 뒤 · 반송 확인

주소가 틀린 메일은 **30분에서 1시간쯤 지나** 되돌아옵니다.
그때 왼쪽 아래 **`반송 확인`**을 눌러 주세요.

되돌아온 주소를 찾아 명단에 `반송됨`으로 표시합니다. 이유도 함께 나옵니다.

```text
하리보     info-kr@haribo.com     없는 계정이에요
신화당     attinino@naver.com     받은편지함이 가득 찼어요
```

표시해 두면 **다음에 보낼 때 자동으로 건너뜁니다.**

---

## 폴더 구조

평소에 열어볼 일이 있는 건 **행사별 작업**과 **기업 데이터** 두 곳뿐입니다.

```text
대외협력국 업무 자동화 툴/
│
├─ 실행하기.bat           ★ 더블클릭하면 켜집니다
├─ run.py                    실행하기.bat 이 부르는 파일
├─ 사용설명서.md          ★ 지금 보고 있는 문서
│
├─ 행사별 작업/           ★ 행사마다 하나씩 생깁니다
│   ├─ 키움/
│   │   ├─ config.json        이 행사의 설정
│   │   ├─ files/             올린 PPT·엑셀 (버전별로 쌓임)
│   │   └─ 제안서/            만들어진 PDF
│   └─ 전간수/
│
├─ 기업 데이터/           ★ 컨택처 자료 모음
│   └─ 정리완료/카테고리별/   식음료·뷰티·주류 등
│
├─ 설정/                  건드릴 일 거의 없음
│   ├─ config.json            Gmail 주소, 마지막 연 행사
│   ├─ credentials.json       구글에서 받은 것 (있어야 연결됨)
│   └─ token.json             연결하면 자동으로 생김
│
├─ 프로그램/              코드. 고칠 일 없으면 안 열어도 됩니다
│   ├─ app.py                 창 틀 · 왼쪽 단계 레일
│   ├─ core.py                설정 · 파일 보관함 · 조사 · 명단
│   ├─ engine.py              PPT 자리 찾기/채우기 · PDF · Gmail · 발송
│   ├─ theme.py               색 · 글꼴 · 공용 위젯
│   └─ screens.py             화면 7개
│
├─ 프로그램 만들기/        exe 만들 때만 씁니다
│   ├─ 배포파일_만들기.bat     더블클릭하면 exe 생성
│   ├─ 메일자동화.spec
│   └─ requirements.txt
│
└─ 예전 파일/             옛 버전. 안 써도 됩니다
```

> **설정**과 **행사별 작업** 폴더는 프로그램이 알아서 만들고 채웁니다.
> 이름을 바꾸면 프로그램이 못 찾으니 그대로 두세요.
>
> `실행하기.bat` 과 `run.py` 만 영어 이름인 이유가 있습니다. 배치 파일(.bat)에 한글이 들어가면
> 쓰는 PC의 언어 설정에 따라 글자가 깨져 실행이 안 됩니다. 그래서 이 두 개만 영어로 두고
> 한글 안내는 전부 프로그램 안에서 합니다.

---

## 자주 막히는 곳

**PPT를 올렸는데 "괄호로 묶인 자리를 찾지 못했어요"**
→ 제안서에서 바꿀 곳을 `(기업명)` 처럼 괄호로 감싼 뒤 다시 올리세요.

**제안서에 `(협찬품)` 글자가 그대로 남아요**
→ 3단계에서 그 자리를 짝짓지 않은 겁니다. 열을 고르거나, 원래 그런 문구라면 그냥 두면 됩니다.

**PDF로 바꾸지 못했어요**
→ 열려 있는 PowerPoint 창을 **모두 닫고** 다시 보내세요. 실패한 곳만 다시 보낼 수 있습니다.

**보낼 곳이 0곳이에요**
→ 2단계에서 이메일 열을 골랐는지, 명단이 전부 `이미 보냄` 상태는 아닌지 확인하세요.
   다시 보내려면 명단의 `발송상태` 칸을 비우면 됩니다.

**한 번에 몇 통까지 보낼 수 있나요**
→ Gmail 무료 계정은 하루 500통입니다. 그보다 많으면 나눠서 보내세요.

---

## exe로 만들기

**프로그램 만들기 / 배포파일_만들기.bat** 을 더블클릭하면 3~10분 뒤 `배포/` 폴더가 생깁니다.

```text
배포/
├─ 메일자동화.exe        더블클릭해서 실행
├─ 사용설명서.md
└─ 설정/
    └─ credentials.json
```

이 `배포` 폴더를 통째로 전달하면 됩니다.

계정 정보가 든 `token.json`과 `config.json`은 **일부러 넣지 않습니다.** 받은 사람이 자기 계정으로
직접 연결하게 하세요. 받아 쓸 때 `설정`과 `행사별 작업` 폴더는 exe 옆에 자동으로 생깁니다.
